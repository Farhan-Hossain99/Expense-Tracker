services:
  - type: web
    name: finpulse
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: finpulse-db
          property: connectionString
      - key: WEB_CONCURRENCY
        value: "4"
